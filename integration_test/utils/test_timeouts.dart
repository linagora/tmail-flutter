abstract final class TestTimeouts {
  static const Duration short = Duration(seconds: 15);
  static const Duration medium = Duration(seconds: 30);
  static const Duration long = Duration(seconds: 90);
}
