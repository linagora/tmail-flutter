name: fcm
description: TwakeMail FCM extension
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  ### Dependencies from git ###
  jmap_dart_client:
    git:
      url: https://github.com/Tsopic/jmap-dart-client.git
      ref: main

  ### Dependencies from pub.dev ###
  equatable: 2.0.8

  json_annotation: 4.9.0

  dio: 5.9.0

  http_mock_adapter: 0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 2.0.1

  build_runner: 2.4.10

  json_serializable: 6.6.1

  mockito: 5.4.4

flutter:
  uses-material-design: true
